## 블룸 필터란 무엇인가?
- 어떤 원소가 집합에 존재할 가능성이 있는지 판별하는 확률적 자료구조
- 틀릴 수 있지만 메모리를 적게 쓰고 속도가 매우 빠르다
### 동작 방식
1. 빈 비트 배열 하나 만든다
2. k개의 해시 함수를 준비한다
3. 원소 X를 삽입할 때 k개의 해시 함수로 나온 인덱스를 1로 설정
4. 어떤 값이 집합에 포함되었는지 확인할 때
	- k개의 해시 값 위치가 모두 1이면 있을 수도 있음
	- 하나라도 0이면 절대 없음